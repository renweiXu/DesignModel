#策略模式
	我的理解就是把某个公共的功能或者行为定义成接口，至于具体怎么做就由相应的类去实现
	这个策略模式的小demo中，把吃这种行为定义成一个接口，具体吃什么就有具体的策略类去完成
	定义一个公共的父类动物类，在动物类中把“吃”接口定义为成员变量，并在构造方法中作为入参
	子类在实例化的时候可以选择具体的“吃”的实现类;策略模式的好处就是可以面向接口编程，扩展性比较好
	但是每当增加一种具体的策略都要加上一个策略类，并且具体的策略类也暴露给调用者了